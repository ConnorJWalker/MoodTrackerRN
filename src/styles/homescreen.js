import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    dayContainer: {
        backgroundColor: '#fff',
        margin: 10,
        borderRadius: 20,
        borderColor: '#444',
        borderWidth: 2,
        padding: 10
    },
    overallMoodContainer: {
        flexDirection: 'row',
        marginBottom: 7
    },
    overallMood: {
        marginTop: 5,
        marginLeft: 3
    }
})